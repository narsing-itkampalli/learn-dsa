var minimizeMax = function(nums, p) {
    nums = nums.sort((a, b) => a - b);
    const n = nums.length;

    for(let i = 1; i < n; i++) {
        
    }

    return nums;
};

console.log(minimizeMax([10,1,2,7,1,3], 2))